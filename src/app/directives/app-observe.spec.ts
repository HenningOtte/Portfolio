import { AppObserve } from './app-observe';

describe('AppObserve', () => {
  it('should create an instance', () => {
    const directive = new AppObserve();
    expect(directive).toBeTruthy();
  });
});
